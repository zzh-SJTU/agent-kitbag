# Plugins

Plugin packages and integrations for coding-agent environments.

Keep each plugin in its own directory:

```text
plugins/
└── plugin-name/
    ├── README.md
    └── ...
```

Document installation, required permissions, external dependencies, and a
minimal usage example.
