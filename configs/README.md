# Configs

Configuration files for coding agents and related tools.

Keep each tool or agent in its own clearly named subdirectory:

```text
configs/
└── tool-name/
    ├── README.md
    └── ...
```

Document where the configuration should be installed and which values users
must customize. Use placeholders for secrets and machine-specific paths.
