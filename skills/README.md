# Skills

Reusable instructions and workflows that give coding agents focused
capabilities.

Keep each skill self-contained:

```text
skills/
└── skill-name/
    ├── SKILL.md
    └── ...
```

The skill README or `SKILL.md` should state when to use it, its prerequisites,
and any scripts or assets it depends on.
